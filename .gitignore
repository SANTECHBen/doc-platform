node_modules
.pnpm-store

# Build outputs
dist
.next
.turbo
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Drizzle
drizzle/meta/_journal.json.bak

# Misc
coverage
*.pem
.vercel

.vercel

# Customer / OEM source manuals — these are the binary artifacts the
# platform serves through R2; they don't belong in git (large, binary,
# bloat history). Keep authored .md / source code under version control.
S25_Operation and Maintenance_Rev 02_.pdf
IntelliSort HDS Maintenance Manual.pdf
IntelliSort HDS Maintenance Manual - sections/

# Debug / scratch env files — only committed scaffolding env is .env.example
.env.neon-debug
.env.*-debug

# Claude Code session state — local tooling artifact, no value to teammates
.claude/scheduled_tasks.lock

# Local reference screenshots (orb mocks, error captures, etc.). Keep them
# on disk but don't ship them — they bloat the repo and rot fast.
ai_icon.jpg
ai_icon*.jpg
ai_display.jpg
ai_display*.jpg
